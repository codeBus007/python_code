链表
和数组相似，链表也是一种线性数据结构，这里是一个例子：
23 -> 6  -> 15 -> x
就像你看到的一样，链表里的元素都是一个分散的对象，但是所有的对象被各自的引用域连接在一起

有两种类型的链表：单向链表和双向链表
这里的内容有：
	*理解单向链表和双向链表的数据结构
	*实现单向链表或双向链表的遍历/插入/删除
	*分析单向链表或双向链表的不同操作的复杂度的分析
	*在链表中使用两指针（快指针-慢指针）的技术
	*实现例如反转链表的的典型问题
	*分析你设计的算法的算法
	*积累设计和debug的经验


